spring:
    profiles:
        active: dev # options: prod，dev，test
    application:
        name: cloud-learning-community-member-service

mybatis-plus:
    global-config:
        field-strategy: 2
        db-column-underline: true
        refresh-mapper: true
        db-config:
            id-type: auto
            table-prefix: t_
    configuration:
        map-underscore-to-camel-case: true
        cache-enabled: false
        return-instance-for-empty-row: true

logback:
    log-dir: /app/member/service/log

server:
    port: 5504
    servlet:
        context-path: /member

gateway:
    service:
        url: http://localhost:5500
